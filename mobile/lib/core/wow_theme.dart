import 'package:flutter/material.dart';

/// Colors sampled directly from assets/main_page.png and assets/
/// splash_screen.png (the supplied design references) - not an
/// approximation of a generic dark theme.
class WowColors {
  WowColors._();

  static const background = Color(0xFF05091C);
  static const surface = Color(0xFF0D1641);
  static const surfaceVariant = Color(0xFF0B1730);
  static const border = Color(0xFF1C2A5E);
  static const primaryBlue = Color(0xFF2F6FED);
  static const accentCyan = Color(0xFF3DD6F5);
  static const accentPurple = Color(0xFF8B5CF6);
  static const success = Color(0xFF1DD693);
  static const textPrimary = Colors.white;
  static const textSecondary = Color(0xFFB7C0E0);
  static const textMuted = Color(0xFF7A84AC);
}
