# Optional dependencies for real text-to-speech (backend/app/providers/tts/local_piper.py).
# Not required for the default SimulatedTTSProvider used elsewhere in Phase 1.
# Install with: pip install -r requirements.txt -r requirements-local-tts.txt
piper-tts>=1.7.0
